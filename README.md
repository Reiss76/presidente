# codes-monorepo

Monorepo que unifica frontend y backend.

## Estructura

- `apps/frontend`: Next.js frontend
- `apps/backend`: Backend API
- `packages`: código compartido (futuro)
- `docs`: documentación

## Siguiente paso

Configurar workspaces (npm/pnpm) y scripts raíz para correr todo desde un solo repo.
